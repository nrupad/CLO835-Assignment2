# TODO: a Service that exposes YOUR app on port 8080 so you can reach it from the host.
# Docs: https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: # TODO
spec:
  type: # TODO  (ClusterIP or NodePort)
  selector:
    app: # TODO  (must match your Deployment's pod labels)
  ports:
    - port: 80
      targetPort: 8080
      # nodePort: # TODO  (optional, 30000-32767)
