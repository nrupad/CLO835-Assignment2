apiVersion: v1
kind: Service
metadata:
  name: clo835-service
spec:
  type: NodePort
  selector:
    app: clo835-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
